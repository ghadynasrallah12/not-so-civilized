{
  "name": "not-so-civilized",
  "version": "1.0.0",
  "private": true,
  "description": "Client portfolio website starter",
  "scripts": {
    "dev": "python3 -c \"import socket,os;s=socket.socket();s.bind(('',0));p=s.getsockname()[1];s.close();print('Serving at http://127.0.0.1:%d/'%p,flush=True);os.execlp('python3','python3','-m','http.server',str(p))\"",
    "start": "python3 -c \"import socket,os;s=socket.socket();s.bind(('',0));p=s.getsockname()[1];s.close();print('Serving at http://127.0.0.1:%d/'%p,flush=True);os.execlp('python3','python3','-m','http.server',str(p))\""
  }
}
